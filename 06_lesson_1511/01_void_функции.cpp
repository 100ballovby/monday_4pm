#include <iostream>
using namespace std;


void do_nothing() {  // функция будет выполнять какие-то действия, но возвращать ничего не будет
    cout << 8 << endl;  // с 8 ничего сделать нельзя, потому что она просто выводится на консоль
}
